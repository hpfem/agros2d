PROJECT(${TRIANGLE_LIBRARY})

add_definitions(-DANSI_DECLARATORS=1)

SET(HEADERS triangle.h)
SET(SOURCES triangle.c)

ADD_LIBRARY(${PROJECT_NAME} ${HEADERS} ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME})
