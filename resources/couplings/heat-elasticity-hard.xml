<?xml version="1.0"?>
<coupling xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="module.xsd">
    <general id ="heat-elasticity" name="Thermo-elasticity">
        <description>Heat plus elasticity</description>
        <modules>
            <source id="heat" analysis="steadystate" />
            <object id="elasticity" analysis="steadystate" />
        </modules>
    </general>

    <volume>
        <weakforms>
            <weakform>
                <matrix i="1" j="2" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdx"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * (vdr + vval / r)" />
                <matrix i="1" j="3" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdy"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdz" />
            </weakform>
        </weakforms>
    </volume>
</coupling>
