<?xml version="1.0"?>
<coupling xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="module.xsd">
    <general id ="heat-elasticity" name="Thermoelasticity">
        <description>Heat plus elasticity</description>
        <modules>
            <source id="heat" />
            <target id="elasticity" />
        </modules>
    </general>

    <volume>
        <weakforms>
            <weakform couplingtype="weak" sourceanalysis="steadystate"  targetanalysis="steadystate" >
                <vector i="1" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdx"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * (vdr + vval / r)" />
                <vector i="2" j="2" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdy"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdz" />
            </weakform>

            <weakform couplingtype="hard" sourceanalysis="steadystate"  targetanalysis="steadystate" >
                <matrix i="2" j="1" planar="((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdx"
                                    axi="r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * (vdr + vval / r)" />
                <matrix i="3" j="1" planar="((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdy"
                                    axi="r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdz" />
                <vector i="2" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdx"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * (vdr + vval / r)" />
                <vector i="3" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdy"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdz" />
            </weakform>


            <weakform couplingtype="weak" sourceanalysis="transient"  targetanalysis="steadystate" >
                <vector i="1" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdx"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * (vdr + vval / r)" />
                <vector i="2" j="2" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdy"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (source - Tr)) * vdz" />
            </weakform>

            <weakform couplingtype="hard" sourceanalysis="transient"  targetanalysis="steadystate" >
                <matrix i="2" j="1" planar="((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdx"
                                    axi="r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * (vdr + vval / r)" />
                <matrix i="3" j="1" planar="((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdy"
                                    axi="r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (uval - Tr)) * vdz" />
                <vector i="2" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdx"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * (vdr + vval / r)" />
                <vector i="3" j="1" planar="- ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdy"
                                    axi="- r * ((3*(E * nu) / ((1.0 + nu) * (1.0 - 2.0*nu)) + 2*E / (2.0*(1.0 + nu))) * alpha * (upval - Tr)) * vdz" />
            </weakform>

        </weakforms>
    </volume>
</coupling>
