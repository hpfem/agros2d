# This is ALMOST a normal Hermes CMake.vars file.
# EXCEPT everything has to be set here (including the defaults in hermes/CMakeLists.txt) - for plugins to be able to cope with just this file.

set(H2D_WITH_GLUT NO)
set(H2D_WITH_TEST_EXAMPLES  NO)
set(TARGET_ROOT ${CMAKE_HOME_DIRECTORY}/libs/hermes)
set(WITH_BSON YES)
SET(BSON_LIBRARY ${BSON_LIBRARY})
set(BSON_INCLUDE_DIR ${CMAKE_HOME_DIRECTORY}/3rdparty/bson)
set(WITH_UMFPACK YES)
set(WITH_MUMPS YES)
set(WITH_PARALUTION YES)
SET(PARALUTION_LIBRARY ${PARALUTION_LIBRARY})
set(PARALUTION_INCLUDE_DIR ${CMAKE_HOME_DIRECTORY}/3rdparty/paralution/src)
set(WITH_TC_MALLOC NO)

# Some search paths.
if (WIN32)
  set(XERCES_ROOT "c:/hpfem/hermes/dependencies")
  set(PARALUTION_ROOT "c:/hpfem/hermes/dependencies")
  set(XSD_ROOT "c:/hpfem/hermes/dependencies")
  set(BSON_ROOT "c:/hpfem/hermes/dependencies")
  set(PTHREAD_ROOT "c:/hpfem/hermes/dependencies")
  set(TCMALLOC_ROOT "c:/hpfem/hermes/dependencies")
  set(WINBLAS_ROOT "c:/hpfem/hermes/dependencies")
  set(UMFPACK_ROOT "c:/hpfem/hermes/dependencies")
  set(MUMPS_ROOT "c:/hpfem/hermes/dependencies")
  set(CLAPACK_ROOT "c:/hpfem/hermes/dependencies")
endif()

if(AGROS_DEBUG)
  set(HERMES_COMMON_DEBUG     YES)
  set(HERMES_COMMON_RELEASE   NO)
  set(H2D_DEBUG               YES)
  set(H2D_RELEASE             NO)
else()
  set(HERMES_COMMON_DEBUG     NO)
  set(HERMES_COMMON_RELEASE   YES)
  set(H2D_DEBUG               NO)
  set(H2D_RELEASE             YES)
endif()

